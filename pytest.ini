[pytest]
markers =
    slow: long-running validation or reproduction tests
    nightly: tests intended for nightly scheduled runs
    regression: reproduction regression tests against paper baselines

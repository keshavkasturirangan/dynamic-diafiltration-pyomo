name: nightly-validation

on:
  schedule:
    - cron: "0 08 * * *"
  workflow_dispatch:

jobs:
  validation-gate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Nightly validation status gate
        run: |
          python scripts/validation/nightly/nightly_validation_gate.py \
            --status-csv docs/validation/target_validation_status_consolidated.csv \
            --exceptions-csv docs/validation/nightly/config/known_nonpass.csv

      - name: Digitized panel comparison (report)
        run: |
          python scripts/validation/nightly/compare_digitized_baselines.py \
            --repo-root . \
            --manifest docs/validation/nightly/digitized_baselines/manifest.csv \
            --thresholds docs/validation/nightly/digitized_baselines/thresholds.csv \
            --out-csv docs/validation/nightly/digitized_baselines/comparison_latest.csv

      - name: Storage-aware prune dry-run
        run: |
          python scripts/reproduce/prune_reproduction_artifacts.py \
            --repo-root . \
            --keep-full 3 \
            --mode archive
